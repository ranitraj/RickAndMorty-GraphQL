query Characters($page: Int) {
    characters(page: $page) {
        results {
            id
            image
            gender
            name
            status
            species
        }
        info {
            count
            next
            pages
            prev
        }
    }
}